# Troubleshooting
Run `pdp doctor`. Database FAIL: stop all processes, copy runtime, run integrity/repair; never fabricate recovery. Model NOT CONFIGURED is valid. Avatar port conflicts require a config change/restart. Control boundary WARN means install/authorize a provider. Audit failure requires investigation before actions.
