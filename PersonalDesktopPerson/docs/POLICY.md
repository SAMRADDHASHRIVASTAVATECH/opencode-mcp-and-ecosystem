# Policy
R0 internal may proceed; R1–R4 require explicit approval in this conservative release. Capability tokens are HMAC-signed, task-scoped, expiring, revocable and one-result-use. External text is untrusted data and never policy authority.
