# Upgrade
Back up first, install the new wheel, then run `pdp repair` and `pdp doctor`. Ordered schema migrations are transactional and preserve event data. Software, database schema, identity and skills have separate versions.
