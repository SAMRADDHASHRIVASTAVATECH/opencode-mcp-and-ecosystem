# Developer guide
Add platform integrations through protocols in `providers.py`. Publish normalized events; never mutate state around the kernel. Add schema changes as ordered migrations. Skills are declarative manifests and cannot embed arbitrary trusted code. Test simulator causality and policy invariants before activation.
