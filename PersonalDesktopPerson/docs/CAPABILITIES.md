# Capabilities
Approved actions receive a capability ID, exact scope, task ID and expiry. Delegated providers must return evidence through `record_action_result`; the token is then revoked. Pause/stop revokes all outstanding tokens.
