# Avatar
The loopback-only web embodiment reads materialized state and displays mode, activity, intention, goal, curiosity and confidence. It is disposable: cognition and identity survive its failure. Native transparent overlays can consume the same state endpoint.
