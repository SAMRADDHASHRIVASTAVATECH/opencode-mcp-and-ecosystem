# Final audit — PADP 2.0
Source compiles; 15 tests pass; wheel archive verifies. The canonical feedback path is executable: event → privacy/trust metadata → world belief → attention → appraisal/affect → intention → persistent plan/task → governor → approval → scoped capability → delegated action → verified result → task transition → experience → memory/reflection. Emergency stop synchronously revokes capabilities and checkpoints SAFE_IDLE. No model can edit immutable identity/policy. External observations are untrusted. OpenCode remains the existing intelligence through MCP `cognitive_context`, plan and result tools; no second generic agent is embedded. Hardware providers not present are reported as WARN/NOT CONFIGURED rather than simulated.
