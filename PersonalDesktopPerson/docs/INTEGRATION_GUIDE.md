# Integration guide
OpenCode is the existing intelligence. It calls PADP tools to enter tasks into the already-running person and uses other MCPs for specialized execution. After an approved delegated action, it must return evidence through `record_action_result`, closing the cognitive feedback loop.
