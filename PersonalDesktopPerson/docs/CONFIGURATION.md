# Configuration
`config/default.json` controls storage, pulse rate, loopback avatar, model endpoint, retention, dialogue, quiet hours and computer-control server. `config/schema.json` documents constraints; runtime validation rejects unknown keys, non-loopback avatar binding, invalid rates/ports and non-conservative mode.
