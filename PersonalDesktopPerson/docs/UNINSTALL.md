# Uninstall
Stop the daemon, remove PADP entries from OpenCode, uninstall the Python package, and retain or delete the runtime directory. `pdp delete-data --yes` irreversibly removes local identity/memory after confirmation.
