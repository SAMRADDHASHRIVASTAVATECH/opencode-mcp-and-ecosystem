# First run
Run installer → `pdp initialize` → edit identity/config → configure optional model → generate OpenCode config → `pdp doctor` → start OpenCode MCP or `pdp start` for standalone presence. External sensors/control remain off until separately authorized.
