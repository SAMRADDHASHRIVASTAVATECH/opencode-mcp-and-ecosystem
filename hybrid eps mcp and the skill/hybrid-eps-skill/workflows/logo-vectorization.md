# Logo workflow
Inspect → compare Canny and Scharr → use low noise blur, morphology close if boundaries have gaps → analyze → filter tiny contours → use moderate simplification → export vector-only SVG first → verify and visually inspect → optionally export hybrid EPS/PDF.
