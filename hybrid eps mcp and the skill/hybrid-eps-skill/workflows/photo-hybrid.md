# Photo hybrid workflow
Inspect orientation/dimensions → compare methods → control edge density aggressively → retain bitmap and overlay only selected contours → plan SVG/PDF/EPS → approve exact output → execute → inspect size/hash and visually review. Pure vector contours do not reproduce photographic color regions.
