# Unknown-file escalation
Identify → inspect 256-byte header → focused format/runtime/architecture checks → route normal → extract bounded strings → inspect as container only if signature supports it → discover capability tools → approve a curated install only if essential → run deep pipeline → review uncertainty and manifest.
