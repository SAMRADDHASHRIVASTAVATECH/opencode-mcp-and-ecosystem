import{describe,it,expect}from'vitest';import{writeFileSync,mkdirSync}from'node:fs';import path from'node:path';import{Approval,planDigest}from'../src/approval/engine.js';import type{MediaPlan}from'../src/types.js';const base:Omit<MediaPlan,'digest'>={id:'p',operation:'x',inputs:[],outputs:[],argv:['a'],executable:'/bin/echo',network:false,overwrite:false,persistentEffects:[],potentialDataLoss:[],verification:[],createdAt:1};describe('approval',()=>{it('exact and single use',()=>{const p={...base,digest:planDigest(base)},a=new Approval(),t=a.issue(p,'approve').token;a.consume(p,t);expect(()=>a.consume(p,t)).toThrow()});it('rejects changes',()=>{const p={...base,digest:planDigest(base)},a=new Approval(),t=a.issue(p,'yes').token;expect(()=>a.consume({...p,argv:['evil']},t)).toThrow('PLAN_CHANGED')});it('rejects vague confirmation',()=>{const p={...base,digest:planDigest(base)};expect(()=>new Approval().issue(p,'okay')).toThrow()})});describe('paths',()=>{it('rejects traversal dynamically',async()=>{const{safePath}=await import('../src/security/paths.js');expect(()=>safePath('../../etc/passwd')).toThrow()})});
