import{createHash,randomBytes,timingSafeEqual}from'node:crypto';import type{MediaPlan}from'../types.js';const stable=(x:any):string=>Array.isArray(x)?`[${x.map(stable)}]`:x&&typeof x==='object'?`{${Object.keys(x).sort().map(k=>JSON.stringify(k)+':'+stable(x[k])).join(',')}}`:JSON.stringify(x);export function planDigest(p:Omit<MediaPlan,'digest'>){return createHash('sha256').update(stable(p)).digest('hex')}export class Approval{private tokens=new Map<string,{digest:string,expires:number,used:boolean}>();issue(p:MediaPlan,confirmation:string){if(!/^(approve|approved|yes)$/i.test(confirmation.trim()))throw Error('EXPLICIT_APPROVAL_REQUIRED');const token=randomBytes(32).toString('base64url'),expires=Date.now()+60_000;this.tokens.set(token,{digest:p.digest,expires,used:false});return{token,planId:p.id,digest:p.digest,expiresAt:expires}}consume(p:MediaPlan,token:string){const r=this.tokens.get(token);if(!r||r.used||r.expires<Date.now())throw Error('APPROVAL_INVALID_EXPIRED_OR_USED');const{digest,...unsigned}=p;const a=Buffer.from(planDigest(unsigned)),b=Buffer.from(r.digest);if(a.length!==b.length||!timingSafeEqual(a,b))throw Error('PLAN_CHANGED');r.used=true}}
