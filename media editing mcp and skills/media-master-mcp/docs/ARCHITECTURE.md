# Architecture
OpenCode skill → MCP tools → capability discovery → constrained planner → exact approval → argv executor → bounded job → output verification. Plans are stored only in memory. Jobs and approvals do not survive restart. Backends are dynamically located through the host executable search path.
