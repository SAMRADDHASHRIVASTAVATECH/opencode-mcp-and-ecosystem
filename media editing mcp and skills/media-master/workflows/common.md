# Common workflows
Image: probe → plan conversion/resize/strip → approve → execute → probe dimensions/format/metadata.
Video: probe streams/duration → plan transcode/trim/thumbnail/audio extraction → approve → execute → probe output.
Download: establish authorization → discover yt-dlp → plan HTTPS single item → approve network and output effects → execute → probe downloaded media.
