# Quick routing

Inspect status/capability first. Prefer live official MCP tools for Workflow SDK/node definitions/agents/data tables. Use n8n-full semantic workflow and execution tools for normalized fallback and approval. REST fills lifecycle/admin gaps. Offline is artifact analysis only.

Every mutation or execution follows: inspect → validate/design → operation_plan → show user → explicit answer → operation_approve → matching action tool → verify.
