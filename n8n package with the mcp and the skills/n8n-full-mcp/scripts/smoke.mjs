import{Client}from'@modelcontextprotocol/sdk/client/index.js';import{StdioClientTransport}from'@modelcontextprotocol/sdk/client/stdio.js';
const t=new StdioClientTransport({command:process.execPath,args:['lib/index.js'],env:{...process.env,N8N_MODE:'offline',N8N_OFFLINE_DIR:'/tmp/n8n-full-mcp-smoke'}});const c=new Client({name:'smoke',version:'1.0.0'});await c.connect(t);const tools=await c.listTools();if(tools.tools.length<20)throw Error(`only ${tools.tools.length} tools`);const status=await c.callTool({name:'n8n_status',arguments:{}});console.log(JSON.stringify({initialized:true,toolCount:tools.tools.length,statusContent:status.content?.[0]?.type}));await c.close();
