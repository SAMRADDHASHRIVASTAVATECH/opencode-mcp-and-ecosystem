import{createHash,randomBytes,timingSafeEqual}from'node:crypto';import type{ApprovalRecord,Operation}from'../types.js';
const stable=(x:unknown):string=>Array.isArray(x)?`[${x.map(stable).join(',')}]`:x&&typeof x==='object'?`{${Object.entries(x as Record<string,unknown>).sort(([a],[b])=>a.localeCompare(b)).map(([k,v])=>JSON.stringify(k)+':'+stable(v)).join(',')}}`:JSON.stringify(x);
export class ApprovalEngine{private records=new Map<string,ApprovalRecord>();constructor(private ttlMs=60000){}describe(o:Operation){return{...o,approvalRequired:o.risk!=='read',prompt:'Approve this exact operation?'}}issue(o:Operation,answer:string){if(!/^(approve|approved|yes)$/i.test(answer.trim()))throw new Error('Explicit approval not provided');const digest=createHash('sha256').update(stable(o)).digest('hex');const token=randomBytes(32).toString('base64url');this.records.set(token,{token,digest,expiresAt:Date.now()+this.ttlMs,used:false});return{approvalToken:token,expiresAt:Date.now()+this.ttlMs,operationDigest:digest}}consume(o:Operation,token?:string){if(o.risk==='read')return;const r=token?this.records.get(token):undefined;if(!r||r.used||Date.now()>r.expiresAt)throw new Error('Approval is missing, expired, or already used');const d=createHash('sha256').update(stable(o)).digest();const expected=Buffer.from(r.digest,'hex');if(d.length!==expected.length||!timingSafeEqual(d,expected))throw new Error('Approval does not match the exact operation');r.used=true}}
