import type{Config,Operation,ToolResult}from'./types.js';import{RestAdapter}from'./adapters/rest.js';import{OfflineAdapter}from'./adapters/offline.js';import{OfficialMcpAdapter}from'./adapters/official-mcp.js';
export class Router{rest:RestAdapter;offline:OfflineAdapter;official:OfficialMcpAdapter;constructor(public config:Config){this.rest=new RestAdapter(config);this.offline=new OfflineAdapter(config.offlineDir);this.official=new OfficialMcpAdapter(config)}async read(officialTool:string|undefined,args:Record<string,unknown>,api?:{path:string;query?:Record<string,unknown>},offline?:()=>Promise<ToolResult>):Promise<ToolResult>{if(this.official.available()&&officialTool){const r=await this.official.call(officialTool,args);if(r.ok)return r}if(this.rest.available()&&api){const r=await this.rest.request('GET',api.path,undefined,api.query);if(r.ok)return r}if(offline)return offline();return{ok:false,backend:'router',error:{code:'unavailable',message:'No configured backend supports this capability'}}}async write(officialTool:string|undefined,args:Record<string,unknown>,api?:{method:string;path:string;body?:unknown},offline?:()=>Promise<ToolResult>):Promise<ToolResult>{if(this.official.available()&&officialTool){const r=await this.official.call(officialTool,args);if(r.ok)return r}if(this.rest.available()&&api){const r=await this.rest.request(api.method,api.path,api.body);if(r.ok)return r}if(offline)return offline();return{ok:false,backend:'router',error:{code:'unavailable',message:'No configured backend supports this operation'}}}}
