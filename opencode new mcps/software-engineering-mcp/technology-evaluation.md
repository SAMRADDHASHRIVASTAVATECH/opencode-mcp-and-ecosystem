# SE MCP — Technology Evaluation

| Choice | Why |
|--------|-----|
| Python + official `mcp` SDK | Matches sibling specialists |
| Data-driven stack catalog | Not MERN-hardcoded |
| Marker-based project discovery | Language-agnostic |
| Skills as SKILL.md | Progressive disclosure, portable |
| Route table in code + skill | MCP executes; skill teaches when |

Rejected: rebuilding Android Gradle inside this MCP; wrapping `bash -c user_string`.
