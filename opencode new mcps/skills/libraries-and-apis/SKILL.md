---
name: libraries-and-apis
description: Builds reusable libraries and HTTP services with explicit public APIs. Use when the deliverable is a package or backend, not a full product UI.
---

# Libraries and APIs

- Library: `python-lib` / `node-library`. Public functions documented; no CLI side effects in import.
- API: health route, versioning later, Database MCP if persistence is SQL.
- Semantic versioning from 0.1.0 until a real contract exists.
