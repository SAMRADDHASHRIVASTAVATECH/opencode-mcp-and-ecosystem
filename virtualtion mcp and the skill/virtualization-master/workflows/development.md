# Development workflow
Select exact guest → verify power and guest control → detect OS/toolchain/package manager → assess network need → snapshot if rollback is feasible → approve each mutation boundary → install/copy/build/test/run → verify process, port and application response → report persistent changes and rollback path.
