# Virtualization Master OpenCode skill
Install this directory as an OpenCode skill and configure the companion MCP using `examples/opencode.json`. The skill is a reasoning layer; it never claims capabilities beyond the MCP's dynamic discovery and planner.
