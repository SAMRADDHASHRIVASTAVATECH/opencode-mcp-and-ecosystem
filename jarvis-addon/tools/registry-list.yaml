tool_id: registry-list
name: registry-list
version: 1.0.0
description: list registry contents read-only
inputs: []
outputs: []
permissions:
  auth_level: SAFE_EXECUTE
provider: host-adapter
verification:
  needs_evidence: true
failure_behavior: typed failure
health:
  checkable: true
