tool_id: schema-validate
name: schema-validate
version: 1.0.0
description: validate against a JSON schema
inputs: []
outputs: []
permissions:
  auth_level: SAFE_EXECUTE
provider: host-adapter
verification:
  needs_evidence: true
failure_behavior: typed failure
health:
  checkable: true
