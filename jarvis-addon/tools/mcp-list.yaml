tool_id: mcp-list
name: mcp-list
version: 1.0.0
description: list discovered/installed MCP servers read-only
inputs: []
outputs: []
permissions:
  auth_level: SAFE_EXECUTE
provider: host-adapter
verification:
  needs_evidence: true
failure_behavior: typed failure
health:
  checkable: true
