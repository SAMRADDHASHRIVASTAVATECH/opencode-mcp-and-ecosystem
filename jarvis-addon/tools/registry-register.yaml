tool_id: registry-register
name: registry-register
version: 1.0.0
description: register an entry (authorized)
inputs: []
outputs: []
permissions:
  auth_level: SAFE_EXECUTE
provider: host-adapter
verification:
  needs_evidence: true
failure_behavior: typed failure
health:
  checkable: true
