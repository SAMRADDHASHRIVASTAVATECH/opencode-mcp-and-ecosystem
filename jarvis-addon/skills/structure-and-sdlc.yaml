skill_id: structure-and-sdlc
name: Structure And Sdlc
version: 1.0.0
description: Turns ASCII file trees and the 21-phase SDLC catalog into real project
  layouts. Use when the user pastes a tree, wants boilerplate folders, or asks for
  an SDLC blueprint by audience and project type.
purpose: Turns ASCII file trees and the 21-phase SDLC catalog into real project layouts.
  Use when the user pastes a tree, wants boilerplate folders, or asks for an SDLC
  blueprint by audience and project type.
inputs:
- task description
outputs:
- completed work package
preconditions:
- request maps to capability
postconditions:
- work verified
permissions:
  auth_level: AUTHORIZED_AUTONOMOUS
dependencies: []
required_capabilities:
- software-engineering
tools:
- se_mcp.tools
- mcp-routing
execution_contract:
  inputs:
  - task description
  outputs:
  - completed work package
verification_contract:
  checks:
  - result.perform_verification()
failure_contract:
  failure_modes:
  - unverified result
recovery_contract:
  strategies:
  - classify/retry/rollback
examples: []
tests: []
status: registered
