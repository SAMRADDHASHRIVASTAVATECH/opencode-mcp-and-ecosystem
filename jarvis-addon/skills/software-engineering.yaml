skill_id: software-engineering
name: Software Engineering
version: 1.0.0
description: Orchestrates a one-person software team workflow from request to verified
  delivery. Use when the user asks to build, maintain, debug, or ship any application,
  library, API, CLI, desktop, or service — and before writing code.
purpose: Orchestrates a one-person software team workflow from request to verified
  delivery. Use when the user asks to build, maintain, debug, or ship any application,
  library, API, CLI, desktop, or service — and before writing code.
inputs:
- task description
outputs:
- completed work package
preconditions:
- request maps to capability
postconditions:
- work verified
permissions:
  auth_level: AUTHORIZED_AUTONOMOUS
dependencies: []
required_capabilities:
- software-engineering
tools:
- se_mcp.tools
- mcp-routing
execution_contract:
  inputs:
  - task description
  outputs:
  - completed work package
verification_contract:
  checks:
  - result.perform_verification()
failure_contract:
  failure_modes:
  - unverified result
recovery_contract:
  strategies:
  - classify/retry/rollback
examples: []
tests: []
status: registered
