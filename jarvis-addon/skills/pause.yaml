skill_id: pause
name: Pause
version: 1.0.0
description: Pause a long task cleanly, saving state.
purpose: Pause a long task cleanly, saving state.
inputs: &id001
- task id
outputs: &id002
- paused
preconditions:
- task running
postconditions:
- state saved, task paused
permissions:
  auth_level: SAFE_EXECUTE
dependencies: []
required_capabilities:
- long-horizon-autonomy
tools:
- checkpoint-store
execution_contract:
  inputs: *id001
  outputs: *id002
verification_contract:
  checks:
  - state saved
failure_contract:
  failure_modes:
  - save fails
recovery_contract:
  strategies:
  - halt gracefully
examples: []
tests: []
status: validated
