skill_id: libraries-and-apis
name: Libraries And Apis
version: 1.0.0
description: Builds reusable libraries and HTTP services with explicit public APIs.
  Use when the deliverable is a package or backend, not a full product UI.
purpose: Builds reusable libraries and HTTP services with explicit public APIs. Use
  when the deliverable is a package or backend, not a full product UI.
inputs:
- task description
outputs:
- completed work package
preconditions:
- request maps to capability
postconditions:
- work verified
permissions:
  auth_level: AUTHORIZED_AUTONOMOUS
dependencies: []
required_capabilities:
- software-engineering
tools:
- se_mcp.tools
- mcp-routing
execution_contract:
  inputs:
  - task description
  outputs:
  - completed work package
verification_contract:
  checks:
  - result.perform_verification()
failure_contract:
  failure_modes:
  - unverified result
recovery_contract:
  strategies:
  - classify/retry/rollback
examples: []
tests: []
status: registered
