skill_id: mcp-routing
name: Mcp Routing
version: 1.0.0
description: Chooses which existing specialist MCP and which software-engineering
  tools to use. Use at the start of any engineering task and whenever work crosses
  documents, SQL, Windows, Android, or general code.
purpose: Chooses which existing specialist MCP and which software-engineering tools
  to use. Use at the start of any engineering task and whenever work crosses documents,
  SQL, Windows, Android, or general code.
inputs:
- task description
outputs:
- completed work package
preconditions:
- request maps to capability
postconditions:
- work verified
permissions:
  auth_level: AUTHORIZED_AUTONOMOUS
dependencies: []
required_capabilities:
- software-engineering
tools:
- se_mcp.tools
- mcp-routing
execution_contract:
  inputs:
  - task description
  outputs:
  - completed work package
verification_contract:
  checks:
  - result.perform_verification()
failure_contract:
  failure_modes:
  - unverified result
recovery_contract:
  strategies:
  - classify/retry/rollback
examples: []
tests: []
status: registered
