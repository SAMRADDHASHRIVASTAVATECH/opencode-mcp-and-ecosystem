skill_id: tool-fallback
name: Tool Fallback
version: 1.0.0
description: Switch to a fallback tool when the primary fails.
purpose: Switch to a fallback tool when the primary fails.
inputs: &id001
- failure
- alternatives
outputs: &id002
- fallback used or escalated
preconditions:
- alternatives listed
postconditions:
- outcome
permissions:
  auth_level: SAFE_EXECUTE
dependencies: []
required_capabilities:
- high-reliability
tools:
- tool-health
execution_contract:
  inputs: *id001
  outputs: *id002
verification_contract:
  checks:
  - fallback produced result or clean escalate
failure_contract:
  failure_modes:
  - no fallback
recovery_contract:
  strategies:
  - escalate
examples: []
tests: []
status: validated
