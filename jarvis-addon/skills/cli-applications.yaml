skill_id: cli-applications
name: Cli Applications
version: 1.0.0
description: Designs command-line tools with stable exit codes and tests. Use when
  building CLIs in Python, Node, C, Go, Rust, or Java.
purpose: Designs command-line tools with stable exit codes and tests. Use when building
  CLIs in Python, Node, C, Go, Rust, or Java.
inputs:
- task description
outputs:
- completed work package
preconditions:
- request maps to capability
postconditions:
- work verified
permissions:
  auth_level: AUTHORIZED_AUTONOMOUS
dependencies: []
required_capabilities:
- software-engineering
tools:
- se_mcp.tools
- mcp-routing
execution_contract:
  inputs:
  - task description
  outputs:
  - completed work package
verification_contract:
  checks:
  - result.perform_verification()
failure_contract:
  failure_modes:
  - unverified result
recovery_contract:
  strategies:
  - classify/retry/rollback
examples: []
tests: []
status: registered
