skill_id: conflict-resolution
name: Conflict Resolution
version: 1.0.0
description: Resolve conflicting updates/ownership deterministically.
purpose: Resolve conflicting updates/ownership deterministically.
inputs: &id001
- conflict
outputs: &id002
- resolution
preconditions:
- both sides captured
postconditions:
- resolved + logged
permissions:
  auth_level: SAFE_EXECUTE
dependencies: []
required_capabilities:
- multi-agent-coordination
tools:
- merge-service
execution_contract:
  inputs: *id001
  outputs: *id002
verification_contract:
  checks:
  - single merged result
failure_contract:
  failure_modes:
  - unresolvable
recovery_contract:
  strategies:
  - escalate
examples: []
tests: []
status: validated
