skill_id: release-engineering
name: Release Engineering
version: 1.0.0
description: Packages, versions, and verifies software for delivery. Use when the
  user asks to ship, release, produce artifacts, or write install instructions.
purpose: Packages, versions, and verifies software for delivery. Use when the user
  asks to ship, release, produce artifacts, or write install instructions.
inputs:
- task description
outputs:
- completed work package
preconditions:
- request maps to capability
postconditions:
- work verified
permissions:
  auth_level: AUTHORIZED_AUTONOMOUS
dependencies: []
required_capabilities:
- software-engineering
tools:
- se_mcp.tools
- mcp-routing
execution_contract:
  inputs:
  - task description
  outputs:
  - completed work package
verification_contract:
  checks:
  - result.perform_verification()
failure_contract:
  failure_modes:
  - unverified result
recovery_contract:
  strategies:
  - classify/retry/rollback
examples: []
tests: []
status: registered
