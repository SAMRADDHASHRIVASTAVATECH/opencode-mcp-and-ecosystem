skill_id: technology-selection
name: Technology Selection
version: 1.0.0
description: Selects language, framework, and build system from host capabilities
  and requirements. Use before se_create_project or adding dependencies. Avoids MERN-default
  and fashion-driven stacks.
purpose: Selects language, framework, and build system from host capabilities and
  requirements. Use before se_create_project or adding dependencies. Avoids MERN-default
  and fashion-driven stacks.
inputs:
- task description
outputs:
- completed work package
preconditions:
- request maps to capability
postconditions:
- work verified
permissions:
  auth_level: AUTHORIZED_AUTONOMOUS
dependencies: []
required_capabilities:
- software-engineering
tools:
- se_mcp.tools
- mcp-routing
execution_contract:
  inputs:
  - task description
  outputs:
  - completed work package
verification_contract:
  checks:
  - result.perform_verification()
failure_contract:
  failure_modes:
  - unverified result
recovery_contract:
  strategies:
  - classify/retry/rollback
examples: []
tests: []
status: registered
