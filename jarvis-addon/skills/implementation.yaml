skill_id: implementation
name: Implementation
version: 1.0.0
description: How to change and create code professionally in this ecosystem. Use when
  writing or editing application source after a plan exists.
purpose: How to change and create code professionally in this ecosystem. Use when
  writing or editing application source after a plan exists.
inputs:
- task description
outputs:
- completed work package
preconditions:
- request maps to capability
postconditions:
- work verified
permissions:
  auth_level: AUTHORIZED_AUTONOMOUS
dependencies: []
required_capabilities:
- software-engineering
tools:
- se_mcp.tools
- mcp-routing
execution_contract:
  inputs:
  - task description
  outputs:
  - completed work package
verification_contract:
  checks:
  - result.perform_verification()
failure_contract:
  failure_modes:
  - unverified result
recovery_contract:
  strategies:
  - classify/retry/rollback
examples: []
tests: []
status: registered
