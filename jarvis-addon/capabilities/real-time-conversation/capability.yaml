capability_id: real-time-conversation
name: Real-Time Conversation
version: 1.0.0
description: Conversation session/turn/interruption/barge-in/cancel/background-task
  interaction/context continuity/partial IO contracts.
area: real-time-conversation
skills:
- cancel
policies:
- conversation-policy
schemas:
- context
contracts:
- conversation-contract
events:
- VOICE_INPUT
- USER_INTERRUPTED
agents:
- orchestrator
authorization:
  auth_level: SAFE_EXECUTE
operational_building_blocks: defined inline (see referenced skills/policies/schemas/contracts/events)
