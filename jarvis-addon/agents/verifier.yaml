agent_id: verifier
name: Verifier
version: 1.0.0
role: verifier
purpose: Verify results with evidence; never assume tool success.
responsibilities:
- Verify results with evidence; never assume tool success.
allowed_skills:
- verification
- failure-detection
required_capabilities:
- high-reliability
expected_inputs:
- verifier task
expected_outputs:
- universal result
coordination_rules:
- lease-based ownership
- report via universal result
- no conflicting writes without lock
permissions:
  auth_level: SAFE_EXECUTE
